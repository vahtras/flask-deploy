[project]
name = "flask-deploy"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fabric>=3.2.2",
    "flask>=3.1.2",
]

[project.optional-dependencies]
fastapi = [
    "fastapi>=0.121.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.0",
]
